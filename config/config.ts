const config: {
    redis: {
        password: string
        host: string
        port: number
    }
} = {
    redis: {
        password: '',
        host: '',
        port: 11164
    }
}

export default config;
